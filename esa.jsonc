{
  // 阿里云 ESA Pages 部署配置
  "assets": {
    "directory": "dist"
  },
  "routes": [
    {
      // 把 /api/* 请求代理到 givemeoc.com
      "source": "/api/(.*)",
      "target": "https://www.givemeoc.com/wp-json/givemeoc/v1/$1"
    }
  ]
}
